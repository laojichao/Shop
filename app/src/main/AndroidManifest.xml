<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.glut.shop">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <!-- SD卡读写权限 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAG" />


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DesignTheme"
        android:networkSecurityConfig="@xml/network_security_config">
        <activity android:name=".activity.UserActivity" android:theme="@style/AppCompatTheme"></activity>
        <activity android:name=".activity.LaunchActivity" android:theme="@style/AppCompatTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.StoreActivity"
            android:theme="@style/AppCompatTheme">

        </activity>
        <activity
            android:name=".activity.HomeActivity"
            android:theme="@style/AppCompatTheme"/>
        <activity
            android:name=".activity.ClassActivity"
            android:theme="@style/AppCompatTheme" />
        <activity
            android:name=".activity.CartActivity"
            android:theme="@style/AppCompatTheme" />
        <activity
            android:name=".activity.SearchViewActivity"
            android:theme="@style/AppCompatTheme" />
        <activity
            android:name=".activity.SearchResultActvity"
            android:theme="@style/AppCompatTheme">
            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
            </intent-filter>

            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable" />
        </activity>
    </application>

</manifest>